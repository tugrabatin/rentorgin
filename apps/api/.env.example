# Backend API Environment Variables (Netlify Functions)
# Set these in Netlify Dashboard → Site settings → Environment variables

# Database (REQUIRED)
# Use Netlify Postgres or external PostgreSQL (e.g., Supabase, Neon)
DATABASE_URL=postgresql://user:password@localhost:5432/rentorgin?schema=public

# Frontend URL (REQUIRED for CORS)
# Set to your Vercel deployment URL
FRONTEND_URL=https://your-app.vercel.app

# Authentication (REQUIRED)
# Generate: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-minimum-32-characters
JWT_EXPIRATION=15m
REFRESH_TOKEN_EXPIRATION=7d

# Session (REQUIRED)
# Generate: openssl rand -base64 32
SESSION_SECRET=your-session-secret-change-this-minimum-32-characters

# OpenAI (optional)
OPENAI_API_KEY=sk-your-openai-api-key

# Storage
STORAGE_PATH=/tmp
MAX_UPLOAD_SIZE=52428800

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# Environment
NODE_ENV=production
APP_ENV=production
APP_VERSION=0.4.0
APP_NAME=RentOrgin

# Monitoring (optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
